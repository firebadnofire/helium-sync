# SSH key test fixtures

These are public test vectors copied from the dual MIT/Apache-2.0 `ssh-key`
crate's PPK parser tests. They are not production credentials and must never be
used for authentication outside the test suite.
