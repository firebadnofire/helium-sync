{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimal desktop permissions for the Helium Sync client",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:allow-open"]
}
